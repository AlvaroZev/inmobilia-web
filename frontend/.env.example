# Copy to frontend/.env.local for local overrides.

# API base URL. Default uses the Vite dev proxy (/api -> localhost:8080).
VITE_API_BASE=/api

# Production example:
# VITE_API_BASE=https://api.example.com
